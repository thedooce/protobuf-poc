{
  "name": "my-node-typescript-project",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc && npm run copy-protos",
    "copy-protos": "cp -R src/proto dist/proto",
    "start": "node dist/server.js",
    "dev": "ts-node-dev src/server.ts"
  },
  "dependencies": {
    "fastify": "^5.0.0",
    "protobufjs": "^7.4.0"
  },
  "devDependencies": {
    "ts-node-dev": "^1.0.0",
    "typescript": "^5.6.2"
  }
}
